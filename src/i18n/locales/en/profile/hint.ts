// Profile locale chunk: hint
export const enProfile_hint = {
  "user_data.hint.cloudinit_config": "Cloud-init YAML config (typically starts with #cloud-config).",
  "user_data.hint.cloudinit_script": "Cloud-init script (shell script run by cloud-init).",
  "user_data.hint.nixos_configuration": "NixOS configuration fragment (attribute set).",
  "user_data.hint.nixos_flake_configuration": "NixOS flake configuration fragment (attribute set).",
  "user_data.hint.nixos_flake_uri": "Flake URI (e.g. github:user/repo).",
  "user_data.hint.script": "Shell script executed on the VPS. Recommended to start with #!/bin/sh or #!/usr/bin/env bash.",
  "user_data.hint.title": "Format overview",
} as const;
