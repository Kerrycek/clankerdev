// Profile locale chunk: hint
export const csProfile_hint = {
  "user_data.hint.cloudinit_config": "Cloud-init YAML konfigurace (typicky začíná #cloud-config).",
  "user_data.hint.cloudinit_script": "Cloud-init skript (shell skript spuštěný cloud-initem).",
  "user_data.hint.nixos_configuration": "Fragment NixOS konfigurace (atributová sada).",
  "user_data.hint.nixos_flake_configuration": "Fragment NixOS flake konfigurace (atributová sada).",
  "user_data.hint.nixos_flake_uri": "Flake URI (např. github:user/repo).",
  "user_data.hint.script": "Shell skript spuštěný na VPS. Doporučeno začínat #!/bin/sh nebo #!/usr/bin/env bash.",
  "user_data.hint.title": "Přehled formátu",
} as const;
