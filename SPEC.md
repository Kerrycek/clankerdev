# WebUI Next spec entrypoint

The only authoritative WebUI Next specification is:

- `../UI_REDESIGN.md`

Use that file for:
- requirements
- gap tracking
- execution order
- rollout / test gates

This file must remain a pointer only.
Do not add requirements here.
