# Historical / quarantined document

This path is intentionally kept only to avoid broken links.

Its previous contents were removed from active guidance because they encoded superseded UI-mode and workflow assumptions that now conflict with the canonical redesign spec.

## Canonical source of truth

- `../../../UI_REDESIGN.md`

## Rule

- Do not add new requirements here.
- If workflow guidance needs a current derived doc again, regenerate it from the canonical redesign spec.
